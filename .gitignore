data/assistant.db
__pycache__/
*.py[cod]
*.pyo
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
.env
*.env
secrets.py
Claude outputs/
# Spotify OAuth token cache (Phase 6) — holds a refresh token, never versioned.
.spotify_cache

# Runtime logs (Phase 0.5) — rotated on disk, never versioned.
logs/

# Local model weights — large binaries, fetched/quantized locally, not tracked.
models/

# Local AI-agent context (AGENTS.md, generated CODEMAP.md) — never versioned.
.agent/
